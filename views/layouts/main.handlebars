<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Crud Handlebars</title>
</head>
<body>

    <!-- Navbar dinâmica -->
    <nav>
        <a href="/">Home</a>
        {{#if isAuthenticated}}
            <a href="/logout">Logout</a>
        {{else}}
            <a href="/login">Login</a>
            <a href="/register">Registrar</a>
        {{/if}}
    </nav>

    <!-- Conteúdo da página -->
    {{{body}}}

</body>
</html>
